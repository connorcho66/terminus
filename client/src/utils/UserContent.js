import React, { createContext, useContext } from "react";

const userContent = createContext();

const { Provider } = userContent;

const provdeUser = ({ value = [], ...props }) => {
    const [state, dispatch] = use
}